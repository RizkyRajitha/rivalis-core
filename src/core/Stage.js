import Context from './Context'
import Broadcaster from '../structs/Broadcaster'

/**
 * @extends {Broadcaster<Context>}
 */
class Stage extends Broadcaster {

    use(action) {
        
    }
}

export default Stage